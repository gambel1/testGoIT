import  Navigation  from "../Navigation/Navigation";

import { ContainerDiv } from "./AppBar.styled";

export default function NavBar ()  {
  return (
    <ContainerDiv>
      <Navigation />
    </ContainerDiv>
  );
};
